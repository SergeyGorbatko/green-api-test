## Архитектура

Проект — fullstack-приложение:

- Frontend: Vite + React
- Backend: NestJS (прокси)

Backend отдаёт собранный фронтенд как статику и проксирует все запросы к GREEN-API.
Такой подход убирает проблемы с CORS и не позволяет светить API-токены на клиенте.
